UPDATE BUNDLE - preserves history (no data/ folder). Edit .github/workflows/daily.yml
directly on GitHub if your upload tool hides the .github folder.
This update: Scout panel now shows an explicit "No new developments flagged this run -
last checked [date]" healthy state instead of looking empty/broken.
